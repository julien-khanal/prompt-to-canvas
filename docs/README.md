# Docs assets

Drop `screenshot.png` here — the top-level README references `docs/screenshot.png`.

Use one of the run screenshots from a real session (ideally the "3 variations"
workflow with the Final node populated).
